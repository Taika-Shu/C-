#include<iostream>
#include<vector>

int main(){
    int numbers[3] = {1,2, 3}; // Array of integers
    std::cout<<numbers[0]; 
    std::vector<int>nums ={10,20,30};
    nums.push_back(40); // Add an element to the vector

    return 0;
}